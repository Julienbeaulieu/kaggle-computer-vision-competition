from ..models.model_build import BuildMixin
from ..models.model_train import TrainMixin
from ..models.model_restore import RestoreMixin

class BengaliProject(BuildMixin, TrainMixin, RestoreMixin):

    def __init__(self):

        # Load default path,

        # Instantiate model.

        # Train the model.

        # Restore the model

        # Predict using the model
        pass

